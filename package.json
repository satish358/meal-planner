{
  "name": "main",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm run start",
    "build": "cd client/recipe_finder && npm run build",
    "preinstall": "npm run server-install && npm run client-install",
    "client-install": "cd client/recipe_finder && npm install",
    "server-install": "cd server && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
